%Test for background subtraction


function subtractBackground(imageIn, kernel)

outputImage = nan(size(imageIn));

for ii = 1:size(outputImage, 1)
    for jj = 1:size(outputImage, 2)
    
        if ii??
            outputImage(ii) = min(
        else
            
            
            
            
        end

    end
end


end